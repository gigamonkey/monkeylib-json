strings => strings, e.g. "foo" => "foo"
numbers => numbers, e.g. 10 => 10
t, :true => true
nil, :false => false
:null => null

list => {} with keys and values taken pairwise from list. (Empty list is empty object)

vector => [] with elements taken from vector.

